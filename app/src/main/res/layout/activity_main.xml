<?xml version="1.0" encoding="utf-8"?>
<!-- Layout principal du LAB 8 - Majesty ENSA Marrakech -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#F0F4FF">

    <!-- Titre de l'application -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="⚡ Async Lab — GCDSTE S4"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#1A237E"
        android:gravity="center"
        android:paddingBottom="16dp"/>

    <!-- Zone d'affichage du statut du traitement en cours -->
    <TextView
        android:id="@+id/txtStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="⏸ En attente d'une action..."
        android:textSize="15sp"
        android:textColor="#333333"
        android:background="#FFFFFF"
        android:padding="10dp"
        android:layout_marginBottom="14dp"/>

    <!-- Barre de progression (visible uniquement pendant AsyncTask) -->
    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:progress="0"
        android:progressTint="#3F51B5"
        android:visibility="invisible"
        android:layout_marginBottom="14dp"/>

    <!-- Image chargée via le Worker Thread -->
    <ImageView
        android:id="@+id/img"
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp"
        android:src="@mipmap/ic_launcher_round"
        android:contentDescription="Image chargée par le thread"/>

    <!-- Bouton 1 : lance un Worker Thread pour simuler le chargement d'image -->
    <Button
        android:id="@+id/btnLoadThread"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🖼 Charger image (Thread)"
        android:backgroundTint="#3F51B5"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="10dp"/>

    <!-- Bouton 2 : lance une AsyncTask avec barre de progression -->
    <Button
        android:id="@+id/btnCalcAsync"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🧮 Calcul lourd (AsyncTask)"
        android:backgroundTint="#1565C0"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="10dp"/>

    <!-- Bouton 3 : Toast immédiat pour prouver que l'UI reste fluide -->
    <Button
        android:id="@+id/btnToast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💬 Afficher Toast (test réactivité)"
        android:backgroundTint="#283593"
        android:textColor="#FFFFFF"/>

</LinearLayout>